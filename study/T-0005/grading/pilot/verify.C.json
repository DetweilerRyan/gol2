{"skeptic": "C", "verdicts": [
{"issue": "I1", "verdict": "real", "reason": "The board's WIP rule makes a blocked task hold the slot, the plugin half has concrete stall risks (worktree node_modules, trust prompts, session-only checks) that the machine-checkable swap doesn't share, and depends_on is the board's own way to sequence split work.", "evidence": [".board/README.md: \"WIP = 1 per agent. An agent has at most one task in `active/` or `blocked/` at a time. A blocked task still holds the slot\"", ".board/README.md: \"`depends_on` lists task ids. A task isn't `ready/` until all of them are in `done/`.\"", "agentpatterns patterns/agent-design/wip-1-littles-law-agent-throughput.md: \"the agent tended to try to do too much at once\"; its backfire case (\"Tightly bounded micro-tasks\") doesn't apply because the halves are large and verified differently", "T-0001 criterion 9 requires new sessions in the main checkout and a worktree"]},
{"issue": "I2", "verdict": "real", "reason": "By the task's and CLAUDE.md's own statements, node_modules/ is a gitignored bind mount of the main checkout, so a fresh worktree under .claude/worktrees/ has no ${CLAUDE_PROJECT_DIR}/node_modules, and the task never says how the worktree case gets one.", "evidence": ["CLAUDE.md: \"The repo's virtiofs mount silently drops symlinks, so `node_modules/` is a bind mount of `~/.local/share/gol2/node_modules`\"", ".gitignore: \"node_modules/\" and \".claude/worktrees/\"", "T-0001 Context: \"`args: [\"${CLAUDE_PROJECT_DIR}/node_modules/rumdl/bin/rumdl\", \"server\"]`\"", ".claude/agents/coach.md: \"Guards fail closed: if the script can't run (e.g. node_modules missing), the call is blocked.\" This shows the existing hooks have the same dependency."]},
{"issue": "I3", "verdict": "real", "reason": "The board requires every criterion to have a {criterion, command, result} entry from a separate verifier, but 'in a new Claude Code session' names no command, and headless -p runs skip the trust dialog, so which command is used changes the outcome.", "evidence": [".board/README.md: \"a task enters `done/` only when every acceptance criterion has an `evidence` entry (`criterion` copied exactly, the `command` run, and its `result`), and `verified_by` names an agent other than `claimed_by`\"", "T-0001 criterion: \"In a new Claude Code session in the main checkout and in one in a worktree ... `documentSymbol` on a doc returns its headings\"", "claude --help (-p): \"The workspace trust dialog is skipped when Claude is run in non-interactive mode\"", "T-0001 criterion: \"The task's Handoff records ... whether `workspaceSymbol` ... return results\" (it passes whatever the result)"]},
{"issue": "I4", "verdict": "real", "reason": "CLAUDE.md is meant to be the map, but it points only to docs/decisions/ and .board/, so an agent-facing usage doc under docs/tools/ would be reachable only through docs/README.md, which nothing points agents to.", "evidence": ["CLAUDE.md: \"Keep this file a map (under ~100 lines); detail lives in the files it points to.\" Under Where things are, the only doc pointer is \"Decisions and their reasons: [`docs/decisions/`]\"", "docs/README.md: \"`CLAUDE.md` points here\" (it doesn't)", "agentpatterns instructions/agents-md-as-table-of-contents.md, When this backfires: \"Agents do not reliably follow pointers\". A missing pointer makes this worse, not better."]},
{"issue": "I5", "verdict": "real", "reason": "Every finding (parity, gitignore handling, the references cursor behavior that goes into the doc) is stamped 0.2.77, but criterion 2 names no version, and the LSP-behavior claims in the doc aren't re-tested by any criterion.", "evidence": ["T-0001 criterion: \"`package.json` pins `rumdl` exactly in devDependencies\" (no version)", "T-0001 Context: \"rumdl 0.2.77 implements all 53 markdownlint rules\"; \"LSP behavior ... (rumdl 0.2.77, tested over raw LSP against this repo)\"", "T-0001 Context: \"rumdl is 0.x and releases every few days. Pin the exact version and upgrade deliberately.\""]},
{"issue": "I6", "verdict": "real", "reason": "The Context explicitly asks for confirmation that the binary resolves in CI, but no acceptance criterion or evidence entry covers it, and the board counts only criteria as verified.", "evidence": ["T-0001 Context: \"Confirm the binary resolves both in the sandbox ... and in CI.\"", ".github/workflows/check.yml: \"- run: npm ci\" then \"- run: npm run check\"", "T-0001 criterion 13: \"`npm run check` exits 0\" (local)", ".board/README.md: \"Done means verified: ... every acceptance criterion has an `evidence` entry\""]},
{"issue": "I7", "verdict": "not_real", "reason": "The criterion already requires the doc to date each caveat by Claude Code version, and the coach's standing duty to fix or delete docs that disagree with reality covers retirement; formal retire_when metadata is reserved for a scale this repo hasn't reached.", "evidence": ["T-0001 criterion 11: \"It marks any operation the Handoff found not working, with the Claude Code version\"", ".claude/agents/coach.md: \"Keep `docs/` and `README.md` accurate. If a doc disagrees with the code, the code wins: fix or delete the doc.\"", ".claude/agents/coach.md: \"Once CLAUDE.md files hold more than ~50 terminal rules, tag each rule ... `retire_when`\"", ".claude/agents/coach.md: \"apply a practice when its conditions actually hold, not in anticipation\""]},
{"issue": "I8", "verdict": "not_real", "reason": "The gate runs oxfmt --check and the linter over every Markdown file on every commit, so a real formatter/linter conflict shows up as a red gate; the extra fixture guards only against a hypothetical future oxfmt change, and pinned tools change only through a new ADR.", "evidence": ["package.json check: \"npm run -s format:check && npm run -s lint && npm run -s lint:md ...\"", "docs/decisions/0001-stack.md: \"Tool versions are pinned exactly in `package.json`. Changing any of these tools needs a new ADR.\"", ".claude/agents/coach.md: \"apply a practice when its conditions actually hold, not in anticipation\""]},
{"issue": "I9", "verdict": "not_real", "reason": "The feared regression requires a config edit or a deliberate pinned upgrade, which the ADR process and the task's 'upgrade deliberately' already gate, and the markdownlint relative-links setup it replaces had no lasting fixture either, so adding one is anticipatory.", "evidence": ["docs/decisions/0001-stack.md: \"Tool versions are pinned exactly ... Changing any of these tools needs a new ADR.\"", "T-0001 Context: \"Pin the exact version and upgrade deliberately.\"", "snapshot scripts/ contains only check-board.ts (no Markdown link fixture exists today)", ".claude/agents/coach.md: \"apply a practice when its conditions actually hold, not in anticipation\""]},
{"issue": "I10", "verdict": "real", "reason": "The repo's own guidance says to adopt a practice when its conditions hold, not in anticipation, and the task reports only 7 Markdown files and a known bug that may disable 3 of the 5 LSP operations, so asking the user to confirm the need before moving it out of backlog/ is reasonable.", "evidence": [".claude/agents/coach.md: \"The repo is meant to grow large with many agents, but apply a practice when its conditions actually hold, not in anticipation.\"", "T-0001 Context: \"On the 7 current files, rumdl takes ~0.07 s\"", "T-0001 Context: \"`workspaceSymbol`, `goToDefinition`, and `findReferences` return nothing while `hover` and `documentSymbol` work\"", ".board/README.md: \"`backlog/` | Captured, not yet specified\""]},
{"issue": "I11", "verdict": "not_real", "reason": "The Context's 'writes or reviews' already allows the implementer to write the doc and the coach to review it, and the suggested fix conflicts with the coach role, which may not write to board lanes other than retros/ and so can't claim a task.", "evidence": ["T-0001 Context: \"The coach maintains `docs/`, so the coach writes or reviews the `docs/tools/` doc.\"", ".claude/agents/coach.md: \"In `.board/`, write only to `retros/`. The other lanes and artifacts belong to task management.\"", ".claude/agents/coach.md Scope Exclusions: \"Task management on `.board/` (other than retros) → whoever owns the board\""]},
{"issue": "I12", "verdict": "unclear", "reason": "rumdl does flag default-rule issues on the reference pages the coach routinely reads (75 on two pages), but the material doesn't show whether Claude Code surfaces LSP diagnostics for files that are only Read outside the project.", "evidence": ["rumdl 0.2.77 check on agentpatterns instructions/agents-md-as-table-of-contents.md and wip-1 page: \"Found 75 issues in 2 files\" (MD013 etc.; the clone has no rumdl/markdownlint config)", ".claude/agents/coach.md: \"agentpatterns.ai is cloned at `../agentpatterns` ... Read a page ... before citing it.\"", "No material documents Claude Code's diagnostic delivery on Read-only files"]},
{"issue": "I13", "verdict": "not_real", "reason": "The repo's precedent treats agent-environment config (hooks in .claude/settings.json) as outside the ADRs, the linter swap that ADR 0001 requires is already covered, and user approval of a task is recorded by its move to ready/.", "evidence": ["docs/decisions/0001-stack.md mentions `.claude/hooks/` only as \"TypeScript too, run with `tsx`\"; no ADR records adding the Stop/SubagentStop hooks", "docs/decisions/0001-stack.md: \"Changing any of these tools needs a new ADR.\" This is satisfied by T-0001 criterion 1.", "git context: \"d4a58d4 Approve T-0005: move to ready\" shows approval is recorded by moving the task to ready/"]},
{"issue": "I14", "verdict": "not_real", "reason": "The same Context paragraph gives the gitignored-file condition and says worktree sessions 'may be fine', and criterion 10 asks the implementer to record whether each operation returns results rather than expecting failure, so a success wouldn't be read as a setup mistake.", "evidence": ["T-0001 Context: \"wrongly drops all of them when the queried file is gitignored\"", "T-0001 Context: \"From inside a worktree, `git check-ignore` says its files aren't ignored, so worktree sessions may be fine, but that's untested.\"", "T-0001 criterion 10: \"records ... whether `workspaceSymbol`, `goToDefinition`, and `findReferences` return results\""]},
{"issue": "I15", "verdict": "unclear", "reason": "Nothing in the allowed material shows a user-level rumdl plugin, and criterion 8 already requires showing that the server runs from the project's node_modules rather than a global install, which partly guards against this.", "evidence": ["T-0001 criterion 8: \"The plugin starts the rumdl version pinned in `package.json` from the project's `node_modules/`, not a global install\"", "T-0001 Context mentions no existing user-level plugin; `claude plugin list` output is not in the permitted material"]},
{"issue": "I16", "verdict": "not_real", "reason": "The Context already says to verify the second-hand details while implementing and names the same checks (claude plugin list, --debug), and the board's blocked/ lane with a Handoff is the existing stop point.", "evidence": ["T-0001 Context: \"Plugin setup (from the Claude Code docs, via a research subagent; verify while implementing)\"", "T-0001 Context: \"Check loading with `claude plugin list` or `/plugin`, and `claude --debug` for server start errors.\"", ".board/README.md: \"`blocked/` | Can't proceed; the body says why and what unblocks it\"; \"Handoff: when a task leaves `active/`, update its `## Handoff` section\""]},
{"issue": "I17", "verdict": "real", "reason": "The Stop/SubagentStop hooks in .claude/settings.json are one of the gate's named enforcement points, Claude Code silently ignores settings files that fail validation in non-interactive mode, and only a Context note protects them, so a cheap before/after check on .hooks is a reasonable criterion.", "evidence": ["CLAUDE.md: \"`npm run check` is the gate. It runs as the git pre-commit hook, in CI, and when the main agent or a subagent stops after changing files (Stop/SubagentStop hooks).\"", "CLAUDE.md: \"Never: bypass the gate with `--no-verify` or by disabling hooks.\"", "claude --help: \"Settings files that fail validation are silently ignored in this mode (no error dialog is shown).\"", "T-0001 Context: \"`.claude/settings.json` already holds the project's hooks; add to it without disturbing them.\" This is a note, not a criterion."]},
{"issue": "I18", "verdict": "not_real", "reason": "The plugin is committed in this repo, so any later hook or component would arrive as a reviewed diff; guarding against a hypothetical future addition is the anticipatory practice the repo's guidance warns against.", "evidence": ["T-0001 criterion 7: \"A Claude Code plugin committed in the repo\"", ".claude/agents/coach.md: \"apply a practice when its conditions actually hold, not in anticipation\""]},
{"issue": "I19", "verdict": "real", "reason": "The reference documents that project-scope plugins' LSP servers start only after workspace trust and face extra approval, so 'a new session needs no manual plugin install' could hit a prompt, and checking this while implementing is a cheap response.", "evidence": ["agentpatterns tools/claude/local-plugin-scaffolding.md: \"LSP servers start only after you trust the workspace\"", "agentpatterns tools/claude/local-plugin-scaffolding.md: \"Project-scope `@skills-dir` plugins load only after the workspace trust dialog; MCP and LSP servers face extra approval\"", "T-0001 criterion 7: \"so a new session needs no manual plugin install\""]}
]}
