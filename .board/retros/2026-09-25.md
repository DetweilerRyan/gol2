# Retro 2026-09-25: session 440265dc (rumdl / Markdown LSP board work)

## Prior predictions

None; this is the first retro.

## Episodes

Line numbers refer to the main transcript `440265dc-29df-4a06-99dc-0fa759d9950d.jsonl`.

| #   | Where                                     | Symptom               | Layer                      | Evidence                                                                                                                                                                                                                                                                                                                            |
| --- | ----------------------------------------- | --------------------- | -------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| E1  | L461–495                                  | S4                    | context                    | T-0001 was written straight into `ready/`. User: "you assumed that it was ready, it isn't yet as i want to go over it an approve it". `.board/README.md` still says `ready/` is entered by "whoever specifies it".                                                                                                                  |
| E2  | L500–532                                  | S4                    | context                    | The agent saved that preference to auto-memory. User: "do not save my preference to memory. all material instructions and practices must be documented in the repo."                                                                                                                                                                |
| E3  | L844–884                                  | S2                    | task spec (cause: context) | The first coach task-review prompt carried the main agent's own checklist, and the user stopped it. `coach.md` defines no task-review mode, so the agent improvised one. All 9 later coach prompts used the short pattern-based form, but that form is recorded nowhere.                                                            |
| E4  | L1668, 1808, 2103, 2319, 2422, 2601, 2847 | S2                    | context                    | "present each decision to me one at a time" was asked 7 times, each followed by "go ahead and apply all of them". Every coach review was first presented as one full list.                                                                                                                                                          |
| E5  | L2835, L2994 → L3055                      | S4                    | task spec                  | Coach finding 9 suggested dropping the epic's diagram. The main agent filed that under "other fixes" and applied it with "apply all". User: "i really like the dependancy diagram … please add it back".                                                                                                                            |
| E6  | L1641–2877                                | S4                    | task spec                  | 7 coach review rounds took the board tasks from 48 lines (`1846c06`) to 729 (PR #1). The coach only flagged in round 7 that each round added more than it resolved. The user stopped the reviews (L2877).                                                                                                                           |
| E7  | whole session                             | S6                    | environment                | 20 of 33 failed tool calls were Claude Code's worktree guard refusing Bash commands. Triggers included `.github`, `.gitignore`, `github_pat_`, `source`, `git -C`, `HOME=` and commands built at runtime. This retro hit the guard 4 times.                                                                                         |
| E8  | coach round 6, L2588                      | S1                    | context                    | The T-0002 and T-0007 criteria required the user to approve a PR. That's impossible because agents act on GitHub as the user's account. Checked now: PRs #1–#3 were each authored and merged by the user's account.                                                                                                                 |
| E9  | L3645–3818                                | S3 (user-authorized)  | environment/state          | T-0010's implementer moved it to `done/` with `verified_by: coach` on the user's instruction. The coach can't write to `.board/` outside `retros/`, and no verifier role exists. Board rule: `done/` is entered by "the verifier, never the implementer". The T-0006 session handled the same gap with a separate verifier session. |
| E10 | L908 onward; this run                     | S1                    | context                    | `coach.md`'s transcript folder and `../agentpatterns` path don't resolve from worktrees. Every coach prompt had to pass absolute paths, the coach flagged this in rounds 5 and 6, and this retro's prompt named a folder the transcript had already left.                                                                           |
| E11 | this run                                  | none (harness defect) | environment                | `coach-write-scope.ts` resolves paths against the main checkout. Inside a worktree the coach can only write `CLAUDE.md` files, so this file and `coach.md` were blocked.                                                                                                                                                            |

Checked and not treated as episodes:

- **L553 (worktree not found on the host):** the user found it. The agent's explanation of how host Git treats the worktree was hedged ("presumably") and untested, so nothing is recorded.
- **L807–836:** the `CLAUDE.md` claim that the mount silently drops symlinks was re-verified and holds.
- **Stop-hook failures during the freshness spike:** they came from links broken on purpose, and the agent reported each one honestly.
- **Status reports:** no S7 found; each commit passed the gate.

## Changes

| #   | Change                                                                                                                                                                                       | Layer       | Prediction                                                                                                                       | Pattern                                                        |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------- |
| C1  | `CLAUDE.md` Ask first: moving a task into `.board/ready/` needs the user's approval; draft tasks in `backlog/`.                                                                              | context     | In the next 5 sessions that draft tasks, no task enters `ready/` without the user's approval and no correction like L495 occurs. | anti-patterns/implicit-knowledge-problem.md                    |
| C2  | `CLAUDE.md` Working with the user: record practices in the repo, never in auto-memory.                                                                                                       | context     | No files written under `~/.claude/projects/*/memory/` in the next 5 sessions.                                                    | anti-patterns/implicit-knowledge-problem.md                    |
| C3  | `CLAUDE.md` Working with the user: how to ask the coach for a task review and how to present its decisions (one at a time, then apply fixes once the user agrees); `coach.md` §4 (proposed). | context     | In the next 3 coach task reviews, the user never has to ask for decisions one at a time or correct the review prompt.            | agent-design/persona-as-code.md                                |
| C4  | `coach.md` §4 (proposed): verdict first; decisions kept separate from fixes; removing user-facing content or changing scope counts as a decision; say how much the fixes add.                | task spec   | Each of the next 3 reviewed tasks reaches approval within 3 rounds, and no applied review fix is reverted at the user's request. | anti-patterns/llm-review-overcorrection.md                     |
| C5  | `CLAUDE.md` gotcha: agents act on GitHub as the user's account.                                                                                                                              | context     | None of the next 10 drafted tasks has a criterion that needs a PR review by the user.                                            | context-engineering/discoverable-vs-nondiscoverable-context.md |
| C6  | `CLAUDE.md` gotcha: which Bash forms the worktree guard refuses.                                                                                                                             | environment | At most 5 guard refusals per worktree session over the next 3 worktree sessions (20 in this one).                                | agent-design/five-failure-layers-diagnostic.md                 |
| C7  | `coach.md` (proposed): glob for transcript folders and find sessions by id; absolute agentpatterns path.                                                                                     | context     | The next coach retro or review prompt needs no path override.                                                                    | agent-design/persona-as-code.md                                |
| C8  | `CLAUDE.md`: "Sandbox gotchas" renamed "Environment gotchas", since it now also holds GitHub and Claude Code gotchas.                                                                        | context     | —                                                                                                                                | —                                                              |

**Rule deletion check:** none removed. Each existing line still earns its place:

- The symlink and `node_modules` line was re-verified today.
- A missing `core.hooksPath` fails silently in a fresh clone, so that line isn't discoverable.
- `git status` first: three sessions shared the checkout today.

There are about 17 terminal rules, so lifecycle tags aren't needed yet.

## Metrics

- `CLAUDE.md`: 34 → 50 lines.
- Sessions reviewed: 1 main session, 13 subagents (11 earlier coach runs used as evidence only, 1 research agent, this run excluded) and 6 headless or nested `claude -p` spike sessions. The spike sessions showed no episodes.
- Tool errors in the main session: 33, of which 20 came from the worktree guard.

## Needs the user

1. **Apply the proposed `coach.md` changes** (§4 task reviews, transcript glob, absolute agentpatterns path, description). The write hook blocked the coach from making them.
2. **Fix `coach-write-scope.ts`** (tooling; the coach asks before changing hooks). It should resolve paths against the worktree that contains the target, for example by dropping a leading `.claude/worktrees/<name>/`. Until then, the coach can't write retros or role files from a worktree.
3. **`.board/README.md`** (board owner):
   - Change the `ready/` lane's "Who moves it in" to "the user, or an agent with the user's approval", to match C1.
   - Document epics: membership through the `Epic: T-xxxx.` line, closing only when every task is done or dropped, and a Mermaid dependency diagram for the user.
4. **Decide who verifies board tasks.** Options:
   - (a) A `verifier` role that can set `verified_by` and move tasks to `done/`.
   - (b) The coach verifies read-only, and the rules let the implementer record a named verifier's verdict with a link to its report.
   - (c) A separate session per verification, as T-0006 did.

   As written, T-0010's move breaks the `done/` rule.

5. **Confirm whether "agents open PRs; only the user merges" is a project-wide rule.** It's what happened for PRs #1–#3 and what you decided for T-0002 and T-0007, but it isn't written down anywhere. If you confirm it, the coach adds it to Boundaries.
